//5.1
#include<stdio.h>
int main()
{
    int a,b,c,d;
    float ratio;

    scanf("%d %d %d %d",&a,&b,&c,&d);
    if((c-d)!=0)
    {
    ratio=(float)(a+b)/(float)(c-d); [ratio must be written here]
    printf("%f",ratio);

    }
}
