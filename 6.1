#include<stdio.h>
int main()
{
    int count ,n;
    float x,y;
    y=1.0;
    count=1;
    scanf("%f %d",&x,&n);
    while(count<=n)
          {
              y=y*x;
              count++;
          }
          printf("%f, %d ,%f",x,n,y);

}
