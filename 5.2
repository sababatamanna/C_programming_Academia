#include<stdio.h>
int main()
{
    int count,i;
    count=0;
    float weight,height;
    for(i=1;i<=5;i++)
    {
        scanf("%f %f",&weight,&height);
        if(weight<50 && height >170)
            count=count+1;
    }
    printf("%d",count);

}
